Need to figure out the combined mechanism for all these things very soon:
  Distributed RNG
    Like hash inversion - commit to a value, then publish the hint when challenged.
    If you fail to publish the hint you lose your stake.
  Reward distribution
  Eventual consensus for canonicality
    Weight is based on derived work.
    Propagate CITEs after verifying a few random points inside them.

Verification should informally verify, at randomly chosen points:
  Coin movement
  Contract validity
  Reasonability of compensation
    Run generator, and if we can compute an answer for significantly cheaper than they did, emit our own.
    This protects against weight inflation.
  CITE validity

We may end up with a dependency cycle - maybe we can bootstrap it, as long as it's stable?


Continuous contract results (not just PASS or FAIL) could allow forfeiture of a fraction of collateral.
  This could be useful for timing questions - was data X available by this time?

Local generators can just be a JS function
  This eliminates the possibility of requiring generator attribution (which would be difficult to do anyways)
Run in worker or main thread?
  Run in worker, and also allow async.

Generators need to come from blocks
Go back to launching one worker per generator
