When someone does work for cheaper, the more expensive work should NOT be included in the CITE set.
  This is necessary so overpriced work doesn't influence the derived work.
When an output is consumed twice, the work of each should be multiplied by their mergability probability???

If multiple callers fetch a verifier with internalIncentive, it's added or max-ed?
If multiple callers fetch a verifier with externalIncentive, it's added or max-ed?

Problem:
  It's easy to design a contract that I can solve efficiently, but no one else. So I can create high-work blocks without effort, and influence the derived work.
  Or, work shared with another generator
  Example: Account contracts
  Best solution (not great): imbue each client with a whitelist of fair contract hashes.
  Idea: Only value whitelist or ones that we win
  Idea: Allow generator publishers to profit from speedup?


High-recursion contracts:
  Kill the original contracts; resume them if inputs are satisfied

Executions optionally consume N resources (workers)

Fixed worker pool
  To kill worker, wait 1 second until it blocks and throw, or else terminate.
  Kill if we have a missing input, and subscribe to the input to re-enqueue if the input becomes available.

Don't have a sharp cut-off on recursion distance, but:
  Without any external incentive, represent its score as some factor of the requesting score. Note that the remaining_cpu_time and remaining_blocking_time should be increased to calculate the child as well.




Non-canonical branches should be flagged
  Any double-spends should propagate a canonical:false to all branches except the heaviest.
  A block is canonical if ALL of its inputs are canonical.
  Branches we've built on can be artificially made heavier, which may make them canonical even though peers don't think so. This is fine, and will resolve itself.
  I don't think there's any case when we want to build on a non-canonical branch. We only keep them around in case they become canonical.
    IS THIS TRUE???
  Additionally, if a block only has canonical inputs, we know it's mergable (valid).
  If a block has non-canonical inputs, it may still be mergeable (valid).




Contracts request generators, so work can be reused. Tag blocks with multiple validators (generator and contract). Only need to send out the contract one.

The contract execution could be a block too, with a body of 0 or 1, but it never needs to be broadcasted.

Contracts specify the time within which validation should take. This includes requesting inputs. If the input is unavailable, the contract fails via timeout. Instead of time, complexity is specified, which is defined by RandomX runtime. Unavailability attestations need to be relatively free because availability can be reversed easily.

Perhaps the block request message is unavailability collateral.

Walk graph, weighted by flow. Verify.



Given any block, there needs to be a way to query all "in", or "unfilled" outputs (positive).
Or, alternatively, BlockSet outputs MUST be negative. If an output is positive, the sender must find a block that consumes/fulfills (pays) it and include it in the set.
The problem is this though:
  We can't consider just a single BlockSet, but must consider the entire



Give generators + contracts a hash of the outputs?


What blocks do I want?


To determine canonicality, must sample?

Root question:
  How to determine if the collateral on a block is accurate?
    c/WorkA: {b: 1} -> {AccountX(t=3): 100, AccountX(t=4): -101}
    Recursively:
      Any positive input or output needs to be added to the set. We gotta figure out where the money is coming from.
      This will query blocks with verifier AccountX(t=3)
      Add all blocks to set, and determine canonicality via derived work
      If none are canonical, use one as an input so it starts getting derived work.
      Sample from verifier space, then choose the canonical block.
      Sample from blocks, but ignore if there's a cheaper block with same verifier.
Root knowledge:
  If we get a well-known block (we can ensure this property by broadcasting it),
  And we verify it,
  And it's free-market with verifier V,
  And its reward (sum of outputs) is smaller than any other block with verifier V,
  And sufficient time has passed to allow refutation:
    Then its reward can be interpreted as work.
Vouching for canonicality!!!:
  Canonical (assuming block B is canonical) work derived from some block B is a vouch for canonicality of B!!!
    This depends on a random sampling from new work
  Alternative ideas:
    is_canonical [block_hash] = 1
    This has the same problem as vouching for validity - we still need to validate the collateral.
    However, if the collateral posting is invalid, someone else should flag it or request it.
      Maybe we shouldn't depend on this, because it's turtles all the way down.
    Validate collateral by following it. If we can't get resolution, just abort.
Data availability requests have their collateral multiplied, causing the data in question to be considered missing/invalid until it's provided.
  Cap the multiplied value so we can't submit an unfulfillable request.
    Cap it at 1% of the current total supply?
  For easy-to-verify contracts in general:
    Requestor asks for commitments. C(h, s) = c <-> HASH(plaintext) == h && HASH(plaintext | s | provider_public_key_hash) == c
    The provider gives an initial claim of the validity of his commitment (collateral=1000).
    Requestor challenges with a claim containing his payment (collateral=1).
    In order to not lose his collateral, he must provide the plaintext as a hint.
    It doesn't matter who steals/provides the plaintext, because the requestor claim payment always goes to the provider.

Canonicality: The local property of every ancestor payment having the most derived work, when compared to other payment claims.
  Note that more than one block with verifier V can be canonical, if they don't both claim any single payment.

Any block can be made canonical by re-writing, and not claiming the disputed input(s).

How can we make sure the blocks we're receiving are an accurate sample of the entire network's work?
  Send work to equivalent work
  Send work to everyone
Sample from verifier space, not work space?

Each peer can only create BlockSets containing their own blocks?

Input tree and new block
  How to merge them together?

Block RNG, deterministic from IO
Block RNG, including secret
  I don't think we want this, since I don't want to force bodies to be derived from the RNG
  But is this basically the same as emitCorrect?
Requiring coins from an input
Requiring coins to an output
Block authentication (in addition to just body authentication)

Should collateral consumption be delayed by 10 seconds or so?

Output verifier changes based on claim time?

Idea:
  Output verifier checks the block, and usually delegates to a body checker?
  Claim specifies an output index
  Block doesn't have a verifier anymore
  This is nice since a block without any inputs/claims is just a data block

Honestly collateral can be sent to an input too.
  As long as the block remains canonical, which should happen even if it's invalid, it will lock the input.

When a claim index is negative, you're saying the body does NOT fulfill the output verifier.

Collateral is an output paid to the collateral contract, which prevents spending for 10 seconds following the latest significant contestion.
After 10 seconds:
  It gets paid, and is locked
  Paying it to the wrong place will never be chosen as canonical
  It can be spent by a public key hashing to its param
  Or, if it's invalid, it can be claimed by a correcting block, and those who placed collateral against it
  Spending collateral incorrectly (if your side didn't win) is easily prevented by providing the anti-blocks as a hint.
    Is this true???
  Incorrectly spent collateral should be considered a double-spend and therefore non-canonical.

Draw time diagram of when we can spend the rectification collateral. Can it be before 10 seconds?

Half of the minimum for/against collateral (25% if both are equal) can immediately be spent on rectification (any block that claims the same verifier in dispute).
Only the first 2x the minimum for/against collateral is considered when computing distribution.
Collateral is ordered by order of inclusion on the epoch chain.
  Maybe?
  This also serves the purpose of making sure collateral becomes canonical quickly.
Can we just use the mechanism everything else uses?
  Multi-claims of the same collateral cannot be, and the one with the most derived work becomes canonical.

2 ways for a contract to read the block:
  getBlock(): Uint8Array // Still needs to be parsed
    - Slightly redundant because we're parsing in TS and inside the contract (although we'll need to do this anyways if we want to walk the graph at all)
    + Easier to compute a signature over the entire block
  getInputs(), getOutputs(), getBody(), ...
    - More complex / less abstract interface
    + Better because TS knows which part of the block is used

// Says this body verifies this block's output's verifier
faacf40bcbb561a8f0753bc2b5e5860ccac8cdce2e8f7c93921ac1c2faa9362c: {
  inputs: [...],
  outputs: [{
    amount: 100,
    verifier: {
      contract_hash: 'MATH',
      params: '1+2',
    }
  }],
  body: '',
}

2f7ebcb48efcad0dafe87280cfbedcbde8bfccac36eebec0eba3fbecdbed7dea: {
  inputs: [{
    // amount: 100,
    block_hash: 'faacf40bcbb561a8f0753bc2b5e5860ccac8cdce2e8f7c93921ac1c2faa9362c',
    output_idx: 0,
    // This claim means this block is ONLY valid if '4' fulfills MATH('1+2'),
    //   judged by collateral posted on X??? being greater than collateral posted on Y???
  }],
  outputs: [{
    amount: 100,
    verifier: {
      contract_hash: 'COLLATERAL',
      params: 'side: FOR, public_key: dfd31be503ab0f79a449cdefbaa805509eaf4b6fbf67fecbbd3db7c0754df8b9',
    }
  }],
  body: '4', // Incorrect
}

The output collateral send MUST include all input collateral ledgers, with the correct order, and may have some more entries appended

de67ab4eaad8e63acd2fb0896b9b104cfafbfbf8ab21e3dea0ccdcc9cabbefbb: {
  inputs: [{
    // amount: 50,
    block_hash: '7b2ad8407df3de319ef42a9d8df83def0e6f8e2af3bf0435fbe2ffc2cce110b2', // Account contract
    output_idx: 0,
  }, {
    // amount: 100,
    block_hash: '2f7ebcb48efcad0dafe87280cfbedcbde8bfccac36eebec0eba3fbecdbed7dea', // Collateral FOR contract
    output_idx: 0,
  }],
  outputs: [{
    amount: 25, // Rectification is always considered collateral FOR this block being valid
    verifier: {
      contract_hash: 'RECTIFICATION', // Rectification
      params: 'MATH(1+2)',
    },
  }, {
    amount: 100,
    verifier: {
      contract_hash: 'COLLATERAL',
      params: 'side: AGAINST, public_key: dfd31be503ab0f79a449cdefbaa805509eaf4b6fbf67fecbbd3db7c0754df8b9',
    }
  }, {
    amount: 25,
    verifier: {
      contract_hash: 'COLLATERAL',
      params: 'side: FOR, 5558c5bab6e0f0fbfbbaf2c84ec9acdbeada654f6aaeadaeb3b589e9cefffeea',
      // Can be claimed by peer 'decacb1bd9700b9fcaa195dd2a39a62d31fe6d3d295a209dfe8bb8fc5daf6bf1' if 100 > 50 and 10 seconds have passed
    }
  }],
  body: '/* hint */',
  timestamp: ..., // If timestamp is WITHIN 10 seconds of the collateralizing input's timestamp, the block is collateral with a hint. Otherwise, it's a claim on the finality of the voting.
}

To corroborate a block, publish another block with the same body & collateral
To refute a block, publish


If the challenge multiplier is 10, the AGAINST collateral only needs to be 2 to equal a FOR collateral of 20.
Here, the challenge multiplier is simply 1.

How do we know what the multiplier is? If we're objecting to the body, it's 1. If we're objecting to a hash inversion, it's 1000.
The side is kind of like the multiplier. If it's -1000, we're asking for hash inversion.
Actually it should be claim: Claim, where Claim is an enum specifying what the collateral is claiming.
enum Claim {
  RefPreimage { idx: number },
  InputPreimage { idx: number },
  // OutputContractPreimage { idx: number },
  BodyValidity {},
}
If you specify too low a multiplier (basically, your set price on hash inversions is too high), people simply won't accept your block.
If you specify too low a collateral, people also won't accept your block.

Should rectification be as soon as possible or instantaneous?
  As soon as possible, since we don't want to wait 10 seconds
  Most of the time, clients will have enough coins on hand to double the collateral easily


Flip for/against sides each time the chain reverses? This is so hash inversion requests are handled correctly.

1. Singly-linked collateral chain
  - If only one depositor, can withdrawl immediately, but if more than one, we need an intermediate block
  + Simplest
  LET'S DO THIS ONE
2. Doubly-linked collateral chain (one for FOR and one for AGAINST)
  - If only one/two depositors, can withdrawl immediately, but if more, we need an intermediate block
3. Pay-to-key-linked collateral chain (one for each public key we pay to)
  + Allows immediate withdrawl without an intermediate block
  - Requires logic enforcing collateral inputs (1) all being from the same block and (2) exactly the collateral outputs of that block.
  - Need to pay to the current leader, and include the ledger somewhere else (in the body?)
  - Hard to split up the for/against collateral correctly (we don't know how it's being split up until we know the winner, but we can assume that the current winner is the eventual winner)
  - What happens if the collateral gets split up? Sure, we can invalidate each consumer, but they're still canonical.

What about simulation contracts?
  Contracts can only do hash inversions on inputs
  Can they know the input that requested their verification?
    They can always iterate the inputs and filter them (because there might be more than one)

Peers should help routing blocks to peers who (1) are listening for one of the verifiers in the outputs, or (2) know how to route the block to the listeners (DHT entries).

The source for collateral can be provided in many ways, but typically it's sending an output to any block signed by public key K.
The block posts collateral, saying eventually, if I win, I get to claim the output with my public key K.

For any block, you can ask for hash preimages
For any block, you can contest one of the input verifiers
What does contesting a collateral block do?
  It simply says (1) I want a preimage, or (2) you're not a valid collateral block


// Private key length: 32 bytes
// Full public key length: 65 bytes
// Compressed public key length: 33 bytes
// Signature length: 64 bytes
// Hash length: 32 bytes



NO: Every verified input and pay-from-outputs are considered collateral. Easily verifiable inputs (pay to any block signed by X) is easy collateral.
Outputs paid to the collateral contract are considered collateral.

The outer block validator has only one imported function - hash inversion
  Invert all input hashes, then use those to satisfy contract requests

Each input should specify collateral?

Derived work must ONLY consider work that changes based on the body, not simple refs.

25, 25, 50


What if we could assume that our peers are sampled from the work space, or at least have a work weight property?
I think we just want a work weight property, because we want to choose peers based on ping.

DHT:
  Send blocks with verifier V to the nodes closest to hash(V | i) for i in 1..3



Whenever a block hashes underneath a threshold:
  The hash of all ancestor blocks, each one concatenated with "\0", is the RNG.


Graph of trust?
If all peers 


Checkpoints

Probe


Collect 
What does the signature of a block mean?
  I think it only means something if it's used in verification (for example, account contracts)



Goal: 50KB script; <0.1s bootstrap
