.DS_Store
Thumbs.db
node_modules/
vendor/
dist/
# Well-known block artifacts ship with the package (generated by
# `deno task build:well-known`), so they are tracked despite the `dist/` rule.
!well-known-blocks/*/dist/
npm/
coverage/
lcov.info
render.svg
.claude/worktrees/

# Vite dev-server cache (per-app)
.vite/

# Harness per-run artifacts; committed baselines live in harness/metrics/
runs/
